<?xml version="1.0" encoding="UTF-8"?>
<command>
	<!--
	<command alias="edit" value="open -a MacVim $f" type="system"/>
	-->

	<!--
    I'm having trouble installing a Spotlight searchable MacVim.app using
    HomeBrew. Decided to use Visual Studio Code with VIM plugin instead.

	<command alias="edit" value="/usr/local/bin/mvim -g $f" type="system"/>
	-->

	<!-- 
		Use '\ ' to escape the spaces.
		Single quotes won't work.
		Double quotes would be a mess.
	-->
	<!--
    Enabled VsCodeVim so I got all the VIM goodness.
	VS Code is easier to manage on Mac (installation, launch speed, plugins, etc).
	Decided to use this instead
	-->
	<command alias="edit" value="open -a Visual\ Studio\ Code $f" type="system"/>
</command>
